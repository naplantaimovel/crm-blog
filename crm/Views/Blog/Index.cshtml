@model IEnumerable<crm.Models.BlogArtigo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BlogCategoria.Categoria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Texto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BlogCategoria.Categoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Texto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BlogArtigoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BlogArtigoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BlogArtigoId })
        </td>
    </tr>
}

</table>
