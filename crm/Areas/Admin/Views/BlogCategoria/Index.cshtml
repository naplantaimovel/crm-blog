@model IEnumerable<crm.Models.BlogCategoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BlogCategoriaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BlogCategoriaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BlogCategoriaId })
        </td>
    </tr>
}

</table>
